[global]
; глобальный уровень логов php-fpm
; log_level = warning
error_log = /proc/self/fd/2
;error_log = /var/www/logs/fpm-error.log

[www]
; отключаем access‑логи
; access.log = /proc/self/fd/2
access.log = /dev/null
catch_workers_output = yes

; Режим управления процессами static/dynamic
pm = static

; Максимальное число процессов (наш лимит)
pm.max_children = 45

; Сколько процессов запускается при старте
; pm.start_servers = 20

; Минимальное число простаивающих процессов
; pm.min_spare_servers = 15

; Максимальное число простаивающих процессов
; pm.max_spare_servers = 30

; Через сколько запросов перезапускать процесс (защита от утечек)
pm.max_requests = 1000